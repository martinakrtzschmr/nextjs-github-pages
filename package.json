{
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "deploy": "next build && touch out/.nojekyll && git add . && git commit -m \"Deploy\" && git subtree push --prefix=out https://github.com/martinakrtzschmr/nextjs-github-pages gh-pages"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
